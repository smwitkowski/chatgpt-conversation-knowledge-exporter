# Review API

API service for browsing topics and conversations.

## Setup

```bash
cd apps/review-api
pip install -e .
```

## Run

```bash
python -m review_api.main
# or
review-api
```

The API will run on http://localhost:8000 by default.

## Environment Variables

- `PORT`: Port to run on (default: 8000)
